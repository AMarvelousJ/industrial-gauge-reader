# 工业单针仪表关键点训练与读数泛化交接

更新日期：2026-08-22  
工程目录：`D:\tiaozhanbei\yolo`

## 1. 已锁定的开发结论

北京普龙科技的公开产品以轨道式、轮式智能巡检机器人为主，应用于配电室、铁路信号机械室、综合管廊、泵站、油田、天然气站和石化等工业现场；公开产品说明还明确包含仪表读数、设备外观和泄漏等巡检能力。

来源：

- [北京普龙科技官网](https://www.pulongkj.com/)
- [河北省人工智能产业产品信息](https://gxt.hebei.gov.cn/main/jqrzxcpgl/cpdetail?id=1f4b75e0-18b8-4127-bac1-6522fc39c794)
- [河北日报相关应用报道](https://finance.sina.com.cn/jjxw/2024-11-01/doc-incupefq2297610.shtml)
- [工业设计周企业与场景介绍](https://hidw.com.cn/show/Detail.aspx?id=10003041)

因此，V1优先做工业单针、单刻度仪表：

- 圆形压力、真空、温度和单针差压表；
- 单针色区表；
- 方形/矩形电流表和电压表。

双指针、双刻度、嵌套子表盘、汽车组合仪表、百分表和线性表结构化暂缓。SF6密度、油温/油位等具体表型是根据电力巡检场景作出的工程推断，不是普龙公开的现场仪表清单，必须用企业数据确认。

比赛硬约束仍包括陌生仪表、30种品牌量程、50种类型、RK3568/RK3576端侧时延和功耗等；本阶段240张试验不能宣称达到最终95%/99%指标。比赛原始依据：
比赛原始方案 PDF 保存在项目工作区外部，GitHub 交接仓库不包含个人聊天软件路径或本机私有目录。

## 2. 当前工程状态

现有定位器继续冻结：

```text
runs/detect/meter_yolov8n_final/weights/best.pt
SHA256 A0447F659564955C0FFBCD7BD68394745C9C3CA5686117EFBB41A631CE79E1A1
```

本轮原子提交：

```text
4735223 Extend gauge review for keypoint training
99102aa Add audited pointer keypoint training pipeline
45d9e73 Integrate optional pivot tip pose inference
```

用户原有的三个未跟踪脚本继续保留，不要加入后续提交：

```text
scripts/predict_crop.py
scripts/prepare_dataset.py
scripts/train_detector.py
```

原审核集磁盘快照为8条接受、31条修正、41条暂存；原冻结40条均未审核。`M02_P02_D_0034.png`在旧CSV中仍没有暂存原因，需要用户确认是否是浏览器中尚未保存的修改。

新的`outputs/pointer_keypoint_review_v1`已生成并验证可由标注服务加载：

```text
候选总数       320
目标真值       240
备用候选        80
业务优先       224
泛化候选        96
唯一重复簇     320
保留旧审核      80
保留完成状态    39
```

当前导出器返回`not_ready`是正确行为：旧记录还缺人工针尖、仪表族、物理仪表ID、条件、来源组和品牌型号。自动形态、自动轴心或自动指针只能辅助点击，不能直接当真值。

兼容回归已在未提供关键点权重的情况下完成，结果位于`outputs/style_reader/post_keypoint_compat`：20张定位覆盖20/20，读数17/20（85%），严格18张口径15/18（83.3%），与改造前一致。

## 3. 下一位Agent的执行顺序

### 3.1 完成人工审核

旧标注页可能仍运行在8765，新关键点队列使用8766：

```powershell
cd D:\tiaozhanbei\yolo
.\scripts\run_keypoint_annotation_ui.ps1
```

每条拟进入训练的样本必须满足：

1. `scope_status=in_scope`；
2. 审核状态为`accepted`或`corrected`；
3. 人工确认轴心并重新点击真实主测量针针尖；
4. 填写仪表族、物理仪表ID、条件、来源组、品牌和型号；
5. 保留读数、单位、量程和最小分度；
6. 训练轨道维持总量70%`company_priority`、30%`generalization_guardrail`。

建议仪表族值：`round_pressure`、`round_temperature`、`single_pointer_differential`、`square_ammeter`、`square_voltmeter`、`colored_zone`。条件使用逗号分隔的`normal`、`tilted`、`low_light`、`glare`、`blur`、`occluded`。

同一块物理表使用同一个`physical_meter_id`；同一视频、连续拍摄或网页商品系列使用同一个`source_group`。无法可靠确认品牌或型号时不要编造，优先用备用候选替换该样本。

暂缓状态固定为：

```text
deferred_dual_pointer
deferred_dual_scale
deferred_nested_dial
deferred_automotive
deferred_dial_indicator
deferred_linear
unreadable
other
```

### 3.2 导出并审计240张数据

```powershell
.\.venv\Scripts\python.exe -m pointer_keypoints.dataset `
  --review-csv outputs\pointer_keypoint_review_v1\review.csv `
  --manifest outputs\pointer_keypoint_review_v1\review_manifest.json `
  --validate-only
```

只有该命令返回`validated`后才去掉`--validate-only`生成数据。默认结果是180训练、30验证、30冻结测试，业务优先/泛化保护为168/72。拆分器把物理仪表、采集来源、重复簇和相同品牌型号组成连通泄漏组；同组不得跨拆分。验证集用于阈值，测试集不得调参。

数据导出还会仅为训练集生成30%的模糊、反光和弱光派生图；验证和测试永不使用合成增强。

### 3.3 训练、校准和冻结评测

```powershell
.\.venv\Scripts\python.exe -m pointer_keypoints.train

.\.venv\Scripts\python.exe -m pointer_keypoints.evaluate `
  --weights runs\pointer_keypoints\industrial_single_pointer_v1\weights\best.pt `
  --split val --calibrate `
  --output outputs\pointer_keypoints\val_calibration.json

.\.venv\Scripts\python.exe -m pointer_keypoints.evaluate `
  --weights runs\pointer_keypoints\industrial_single_pointer_v1\weights\best.pt `
  --split test `
  --threshold-file outputs\pointer_keypoints\val_calibration.json `
  --output outputs\pointer_keypoints\frozen_test.json
```

默认模型为`yolov8n-pose.pt`，两个关键点顺序固定为轴心、针尖；输入384，150轮，patience 30，seed 20260822。训练模块会拒绝检测模型并拒绝覆盖已有训练目录。

关键点门槛：轴心中位误差不超过直径3%，针尖不超过5%，方向中位误差不超过2°、P90不超过5°，有效覆盖率至少90%。若失败，先按仪表族和条件分析并补数据，不训练定位YOLO。

### 3.4 接入读数器并回归

```powershell
.\scripts\run_style_reader.ps1 `
  -OutputDir outputs\style_reader\keypoint_v1 `
  -PointerKeypoints runs\pointer_keypoints\industrial_single_pointer_v1\weights\best.pt `
  -KeypointThresholdFile outputs\pointer_keypoints\val_calibration.json
```

读数链现在是：冻结YOLO定位→表盘归一化→关键点轴心/针尖→角度→原刻度映射。关键点通过验证阈值和长度/边界检查时优先接管；否则保留旧几何回退。`predictions.json` 1.0接口不变，诊断增加`keypoint_model`和`geometry_source`。

验收：原20张闭集至少16/20，新30张冻结工业单针测试至少24/30，任何核心仪表族不低于70%。读数正确口径为一个最小分度，缺失时使用1%满量程。

## 4. 企业现场数据申请模板

可直接发给企业或指导老师：

> 为了针对北京普龙巡检机器人的真实场景训练并评测指针仪表读数算法，希望申请一批可授权用于本次比赛研发的原始巡检图片或视频。优先需要配电室、铁路信号房、综合管廊、泵站及油气化工现场中的单指针压力/真空/温度/差压表和方形电流/电压表。数据仅用于离线算法开发，可按要求脱敏，不上传生产系统信息。

每块仪表请求以下材料：

- 原始分辨率图片或视频，保留时间戳和相机信息；
- 唯一物理仪表ID、现场类型和安装位置类别；
- 品牌、型号、单位、量程、最小分度和目标指针；
- 正视、轻/中度斜视、远近距离、正常、弱光、反光、模糊等条件；
- 在安全许可下覆盖不同指针位置，或提供同步DCS/人工复核读数及真值来源；
- 是否允许裁剪、生成增强图、内部训练和比赛现场展示；
- 需要删除的人脸、工号、站点名称、网络地址或设备编号。

目标规模：不少于30个物理仪表/品牌量程组合，每块20–60帧，去重后人工选取240张关键帧。连续帧和同一物理表必须保留关联ID，不能随机拆到训练与测试两侧。

到件验收清单：文件可解码、元数据表能与图片逐条关联、量程/单位/最小分度齐全、真值来源明确、授权和脱敏边界有书面说明。只给表盘图片但没有物理ID或量程真值的数据只能用于预训练，不能用于最终读数准确率。

## 5. 代码与数据边界

- `all_set`只读；不要移动、改名或覆盖。
- `best.pt`保持上述SHA256，不训练、不覆盖。
- 训练权重、数据集、审核输出和可视化均已由`.gitignore`排除。
- 人工真值不得从读数Markdown、文件路径或测试预测反推。
- 新30张测试只由评测器读取；阈值和模型选择只能看训练/验证数据。
- 第三方开源模型和代码继续保留来源及许可证说明。
- 训练完成后才进入模板/OCR刻度改进；本轮不训练OCR、双刻度或多指针模型。

## 6. 迭代状态更新（2026-08-22 关键点首轮）

已完成一次完整闭环：人工标注（235条合格）→ 导出 → 训练 → 校准 → 冻结评测 → 读数器接入。

### 产物
```text
dataset/pointer_keypoints_v1/                     176训练+29验证+30测试+53增强
runs/pose/runs/pointer_keypoints/industrial_single_pointer_v1/weights/best.pt   6.4MB, 3.08M参数
outputs/pointer_keypoints/val_calibration.json   阈值0.75
outputs/pointer_keypoints/frozen_test.json
outputs/style_reader/keypoint_v1/                关键点诊断模式端到端
```

### 结果
- 关键点模型：验证/测试集角度中位误差 6.3°/9.9°（门槛2°），轴心5.9%/8.3%（门槛3%），**未达门槛**。
- 读数端到端（关键点诊断模式，角度一律用旧几何）：**17/20=85%**，严格口径15/18=83.3%，与改造前一致，零回归。
- 关键点**直接接管**会导致读数回归（55%），原因：①模型精度不足；②刻度映射对角度敏感，2°偏差都可能跨过1个最小分度。

### 根因分析（已核实）
- 标注质量：人工轴心/针尖标签基本正确（细线消歧判据与235条标注完全一致）；错误主要在模型侧。
- 模型被**宽箭头配重/阴影**误导，部分预测到配重端；轴心回归精度不足（384分辨率下小轴心难回归准）。
- 方形表（M06 3.1°、M07 5.3°）学得好，圆形表（M01 13.7°、M04 29.7°）学得差。

### 代码变更
- `pointer_keypoints/prefill_review_metadata.py`：元数据预填充（只标轴心+针尖即可训练）；来源组/物理表ID按重复簇粒度（网图批次是异质表集合）；track按来源组分配（70/30不可达时记录relaxed）。
- `pointer_keypoints/dataset.py`：总量自适应（eligible<240时按实际切分）；轨道配比不足时回退实际分布并在审计标记。
- `data_premark/annotation_app.py`：completed仅要求 in_scope 的轴心+针尖；读数/单位/量程不再拦截。
- `pointer_keypoints/inference.py`：新增细线消歧（长细端=读数端，未触发翻转为诊断保留）。
- `style_reader/run_manifest.py`：关键点改为**诊断模式**（记录validated/disagrees，不参与角度与中心）；修复原"无条件覆盖"导致55%回归的问题。

### 下一轮建议（优先级排序）
1. 提升关键点精度到门槛（2°）：更多标注（尤其是圆形压力/色区表硬视角）、imgsz 512、或 yolov8s-pose；达标后可切回"一致接管"语义。
2. 或先攻基线3个失败案例（RG-018/019 M05差压表负值、RG-020 M06电流表），其失败可能在刻度映射而非角度。
3. 企业现场数据申请（30品牌/量程、物理表ID、DCS真值）是最终99%/95%的前提。

## 7. 指针选择语义修复迭代（2026-08-22 晚）

### 结论：诊断精确完成；规则修复未收敛 → 默认关闭，基线无回归

- 基线（关键点诊断模式）：17/20=85%，覆盖100%，83测试全过。
- 3个失败案例根因已精确定位（Lane A/C证据）：
  1. **RG-018/019（M05差压表）**：真实红针被检测到，但①RG-019红针因"离拟合圆心远"被判 `detached_scale_marker` 拒绝；②RG-018红针(164°)分数输给 hough 水平表框线(271°)；③读数容差极严(1-2 Pa)。
  2. **RG-020（M06方形电流表）**：分割掩膜 pca_angle=292.8°（方向正确！）但①因 `pivot_distance=0.44>0.22` 被判 unattached；②即使接管，刻度拟合仍失败（OCR把'0'读成'1'+象限刻度拟合）。
- 共同模式：**盒式表的"拟合圆心≠指针hub"**，导致：真针被误判分离标记、hough水平线（表框/文字）分数虚高、刻度锚点与指针角度原点不一致。

### 已实施的改进（默认关闭/保留）
- `geometry.py`：红针检测增加 **OCR框mask**（净化红色文字污染，2700px→1900px）；输出 extent_ratio/base_point。
- `pointer_semantics.py`：细长红针(elongation≥5)不再被判 detached；evaluation：红针来源无加分（避免圆表误伤）。
- `run_manifest.py`：`_is_frame_line_suspect`（水平长hough线检测）+ `--frame-line-override`（默认OFF，实验性）。
- 注意：开启该覆盖时经验为 17/20 但覆盖率90-95%（RG-018红组件污染未净、RG-019无hub、RG-020刻度拟合失败），**不要开启**。

### 下一轮正确的3个独立修复点
1. **红针hub定位**：输出红组件的 PCA 两端点（pca_base=hub），角度与刻度锚点统一从hub测（RG-018/019都需要）。
2. **RG-020 刻度**：'0'→'1' OCR误读词法约束(量程先验禁止值域外) + 象限弧单调拟合。
3. **掩膜端向消歧**：掩膜pca_pivot端点选择（靠近图像中心≠hub），用红针/径向共识修正。

## 8. 盒式表hub修正（2026-08-22 深夜，已合入）

### 结果：17/20=85%、覆盖100%、strict 15/18、83测试全过（零回归）

### 已合入的修正（全部默认生效，圆表不受影响）
1. **'0'↔'D/O' OCR混淆修正**（`ocr_mapping.py`）：刻度提取时孤立 D/O→0；单调拟合兜底。RG-018刻度拟合从"双模型外推"变为"单一单调模型"（7内点，r²=0.997）。
2. **掩膜hub覆盖**（`run_manifest.py`）：分割掩膜 `minimum_center_distance_ratio>0.25`（偏心轴心信号，盒式表特征）时，用 `pca_pivot` 作为读数原点（原来仅 aspect≥1.35 触发，方形表≈1.0 永远不触发——这才是当年RG-020读错的隐藏原因）。
3. **掩膜候选 hub 一致**：hub覆盖时掩膜 `pivot_connected=True` 且用 `pca_angle`（唯一与hub一致的方向证据）。RG-018 指针方向已修正（325°，从圆心的271.68°水平线错误中脱离）。
4. **hub读数回退**：hub基准刻度拟合失败时回退圆心读数（保住覆盖率100%）。

### 每图状态
- RG-018：方向已对（mask 325° + hub），读数-4.05 vs GT=4（差~8Pa）——剩余误差=**标签与刻度线角度错位(~16°)**。
- RG-019：掩膜未检出（segmenter漏检）→ 维持-2.12；红针(133px, elong 9.5)是唯一可依证据。
- RG-020：方向已对（292.8°），hub刻度拟合失败→回退38.14；需 '0'→'1' 修正 + 拟合容差。

### 下一步（唯一算法目标）
**tick-anchored 刻度映射**（`tick_mapping.py` 已存在但对该表型 no_output）：把OCR标签角**吸附到实际刻度线角**，消除标签错位。需将 analyze_pointer 的分析图gray/圆暴露给主流程，用hub重新计算 `tick_angle_candidates`。这是RG-018/019/020 共同的最后一块拼图。

## 9. 仪表拓扑感知路由 v1（meter_family，2026-08-23 合入）

**模块**：`style_reader/meter_family.py`（纯规则，零训练零标注）。输出4类：`circle / rectangular_box / colored_zone / unknown`。
**接口**：`classify_family(geometry, segmented, dial_geometry, image_meta)`；OCR 不参与（弱辅助=色区像素占比）。
**接入**：`run_manifest.py` 在 segmented 分支后调用，结果写入 `geometry["family"]`；**不改任何读数逻辑**。
**验证**：95测试全过（83+12新增）；20闭集数值字段与 keypoint_v12 基线逐位 diff=0（仅新增 family 字段）；读数 17/20=85%、覆盖100% 不变。

已知限制（如实）：
- RG-002/004 → unknown：dial 几何 roi_fallback(置信0.10)，诚实标记。
- RG-019 → circle：该差压表掩膜漏检、无偏心信号（拓扑上表盘确为圆形），路由器如实回退。
- 色区阈值 0.08 会把带绿色色带的 M01 温度表标为 colored_zone（语义成立，演示可接受）。
